name: Test Arjuna

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build Test Image
        run: |
          docker build --no-cache -f Dockerfile.test -t arjuna-test .